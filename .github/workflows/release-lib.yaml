name: Run release to PyPi (Production)

on:
  push:
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+"

jobs:
  release:
    name: Create Release and Upload to PyPi
    uses: ./.github/workflows/launch-container.yaml
    with:
      command: scripts/release_library